---
name: 'Set up Ansible'
description: 'Set up Ansible'
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v5

    - name: Set up Ansible
      shell: bash
      run: pip3 install ansible
