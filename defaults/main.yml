---
# defaults file for latex_texlive
latex_texlive:
  download_dir_on_remote_host: "{{ download_dir_on_remote_host }}"
  texlive:
    version: "2023"
    install_options: "--paper=a4 --no-doc-install --no-src-install"
  equivs:
    version: "2022"
  remove_texlive_doc: "true"
